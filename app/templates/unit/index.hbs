<DataTable
    @content={{this.model}}
    @fields="name description created"
    @filter={{this.filter}}
    @sort={{this.sort}}
    @page={{this.page}}
    @size={{this.size}}
    @onClickRow={{this.showSource}}/>