<DataTable
    @content={{this.model}}
    @fields="job.source.name name job.finalized"
    @filter={{this.filter}}
    @sort={{this.sort}}
    @page={{this.page}}
    @size={{this.size}}
    @onClickRow={{this.showColumn}}/>